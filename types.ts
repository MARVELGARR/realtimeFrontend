export enum LastSeen {
  EVERYONE,
  MYCONTACTS,
  NOBODY,
}

export enum Precense {
  EVERYONE,
  NOBODY,
}

export enum Gender {
  MALE,
  FEMALE,
  OTHERS,
}

export enum ChatType {
  DIRECT,
  GROUP,
}

export enum GroupRole {
  PARTICIPANT,
  ADMIN,
}
